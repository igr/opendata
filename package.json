{
  "name": "opendata",
  "version": "2.0.7",
  "description": "OpenData",
  "scripts": {
    "clean": "rm -rf out",
    "dev": "node spig.js dev",
    "build": "node spig.js",
    "build:lambda": "cd netlify && build.sh"
  },
  "dependencies": {
    "spignite": "^2.0.7"
  }
}

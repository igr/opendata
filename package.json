{
  "name": "opendata",
  "version": "2.4.1",
  "description": "OpenData",
  "scripts": {
    "clean": "rm -rf out",
    "dev": "node spig.js dev",
    "build": "node spig.js",
    "build:lambda": "cd netlify && build.sh"
  },
  "dependencies": {
    "spignite": "^2.4.1"
  }
}
